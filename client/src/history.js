//inside of here we create and export a history object to use throughout the whole application

//importing the createBrowserHistory from the history package
import { createBrowserHistory } from 'history'; 

//this creates a history object that I can maintain
export default createBrowserHistory();